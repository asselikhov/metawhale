/**
 * Миграционный скрипт для перехода к мультивалютной P2P системе
 * 
 * Этот скрипт выполняет следующие задачи:
 * 1. Добавляет поля currency, pricePerTokenInUSD, totalValueInUSD к существующим P2P ордерам
 * 2. Мигрирует RubleReserve записи в FiatReserve
 * 3. Инициализирует курсы валют в базе данных
 * 4. Проверяет целостность данных после миграции
 */

const mongoose = require('mongoose');
const { connectDatabase, P2POrder, P2PTrade, RubleReserve, FiatReserve, ExchangeRate } = require('../src/database/models');
const fiatCurrencyService = require('../src/services/fiatCurrencyService');

class P2PMigrationService {
  constructor() {
    this.migrationLog = [];
    this.errors = [];
    this.stats = {
      migratedOrders: 0,
      migratedTrades: 0,
      migratedReserves: 0,
      createdExchangeRates: 0
    };
  }

  /**
   * Главная функция миграции
   */
  async runMigration() {
    try {
      console.log('🚀 Начинаем миграцию P2P системы к мультивалютности...\n');
      
      // Подключаемся к БД
      await connectDatabase();
      
      // Этап 1: Миграция ордеров
      await this.migrateP2POrders();
      
      // Этап 2: Миграция сделок
      await this.migrateP2PTrades();
      
      // Этап 3: Миграция резервов
      await this.migrateRubleReserves();
      
      // Этап 4: Инициализация курсов валют
      await this.initializeExchangeRates();
      
      // Этап 5: Проверка целостности
      await this.validateMigration();
      
      // Финальный отчет
      this.printMigrationReport();
      
      console.log('✅ Миграция завершена успешно!');
      process.exit(0);
      
    } catch (error) {
      console.error('❌ Критическая ошибка миграции:', error);
      process.exit(1);
    }
  }

  /**
   * Миграция P2P ордеров
   */
  async migrateP2POrders() {
    try {
      console.log('📦 Этап 1: Миграция P2P ордеров...');\n      
      // Получаем все ордера без поля currency (legacy ордера)\n      const legacyOrders = await P2POrder.find({\n        $or: [\n          { currency: { $exists: false } },\n          { currency: null },\n          { pricePerTokenInUSD: { $exists: false } }\n        ]\n      });\n\n      console.log(`📊 Найдено legacy ордеров: ${legacyOrders.length}`);\n\n      // Получаем курс RUB/USD для конвертации\n      const rubToUsdRate = await fiatCurrencyService.getExchangeRate('RUB', 'USD');\n      \n      if (!rubToUsdRate) {\n        throw new Error('Не удалось получить курс RUB/USD для миграции');\n      }\n\n      console.log(`💱 Используем курс RUB/USD: ${rubToUsdRate}`);\n\n      let migratedCount = 0;\n      \n      for (const order of legacyOrders) {\n        try {\n          // Конвертируем цену в USD\n          const pricePerTokenInUSD = order.pricePerToken * rubToUsdRate;\n          const totalValueInUSD = order.totalValue * rubToUsdRate;\n          \n          // Обновляем ордер\n          await P2POrder.updateOne(\n            { _id: order._id },\n            {\n              $set: {\n                currency: 'RUB',\n                pricePerTokenInUSD: pricePerTokenInUSD,\n                totalValueInUSD: totalValueInUSD,\n                exchangeRateAtCreation: 1 / rubToUsdRate // USD в RUB\n              }\n            }\n          );\n          \n          migratedCount++;\n          \n          if (migratedCount % 100 === 0) {\n            console.log(`  ✅ Обработано ордеров: ${migratedCount}`);\n          }\n          \n        } catch (orderError) {\n          this.errors.push(`Ошибка миграции ордера ${order._id}: ${orderError.message}`);\n        }\n      }\n      \n      this.stats.migratedOrders = migratedCount;\n      console.log(`✅ Мигрировано ордеров: ${migratedCount}\\n`);\n      \n    } catch (error) {\n      throw new Error(`Ошибка миграции ордеров: ${error.message}`);\n    }\n  }\n\n  /**\n   * Миграция P2P сделок\n   */\n  async migrateP2PTrades() {\n    try {\n      console.log('🤝 Этап 2: Миграция P2P сделок...');\n      \n      // Получаем все сделки без поля currency\n      const legacyTrades = await P2PTrade.find({\n        $or: [\n          { currency: { $exists: false } },\n          { currency: null },\n          { pricePerTokenInUSD: { $exists: false } }\n        ]\n      });\n\n      console.log(`📊 Найдено legacy сделок: ${legacyTrades.length}`);\n\n      const rubToUsdRate = await fiatCurrencyService.getExchangeRate('RUB', 'USD');\n      let migratedCount = 0;\n      \n      for (const trade of legacyTrades) {\n        try {\n          // Конвертируем цену в USD\n          const pricePerTokenInUSD = trade.pricePerToken * rubToUsdRate;\n          const totalValueInUSD = trade.totalValue * rubToUsdRate;\n          \n          // Обновляем сделку\n          await P2PTrade.updateOne(\n            { _id: trade._id },\n            {\n              $set: {\n                currency: 'RUB',\n                pricePerTokenInUSD: pricePerTokenInUSD,\n                totalValueInUSD: totalValueInUSD,\n                exchangeRateAtTrade: 1 / rubToUsdRate\n              }\n            }\n          );\n          \n          migratedCount++;\n          \n        } catch (tradeError) {\n          this.errors.push(`Ошибка миграции сделки ${trade._id}: ${tradeError.message}`);\n        }\n      }\n      \n      this.stats.migratedTrades = migratedCount;\n      console.log(`✅ Мигрировано сделок: ${migratedCount}\\n`);\n      \n    } catch (error) {\n      throw new Error(`Ошибка миграции сделок: ${error.message}`);\n    }\n  }\n\n  /**\n   * Миграция рублевых резервов в фиатные резервы\n   */\n  async migrateRubleReserves() {\n    try {\n      console.log('💰 Этап 3: Миграция рублевых резервов...');\n      \n      // Получаем все немигрированные рублевые резервы\n      const rubleReserves = await RubleReserve.find({\n        isMigrated: { $ne: true }\n      });\n\n      console.log(`📊 Найдено рублевых резервов: ${rubleReserves.length}`);\n\n      const rubToUsdRate = await fiatCurrencyService.getExchangeRate('RUB', 'USD');\n      let migratedCount = 0;\n      \n      for (const rubleReserve of rubleReserves) {\n        try {\n          // Создаем новый FiatReserve\n          const fiatReserve = new FiatReserve({\n            userId: rubleReserve.userId,\n            orderId: rubleReserve.orderId,\n            tradeId: rubleReserve.tradeId,\n            amount: rubleReserve.amount,\n            currency: 'RUB',\n            amountInUSD: rubleReserve.amount * rubToUsdRate,\n            exchangeRate: 1 / rubToUsdRate,\n            type: rubleReserve.type,\n            status: rubleReserve.status,\n            reason: rubleReserve.reason,\n            createdAt: rubleReserve.createdAt,\n            releasedAt: rubleReserve.releasedAt\n          });\n          \n          const savedFiatReserve = await fiatReserve.save();\n          \n          // Помечаем старый резерв как мигрированный\n          await RubleReserve.updateOne(\n            { _id: rubleReserve._id },\n            {\n              $set: {\n                isMigrated: true,\n                migratedToFiatReserveId: savedFiatReserve._id\n              }\n            }\n          );\n          \n          migratedCount++;\n          \n        } catch (reserveError) {\n          this.errors.push(`Ошибка миграции резерва ${rubleReserve._id}: ${reserveError.message}`);\n        }\n      }\n      \n      this.stats.migratedReserves = migratedCount;\n      console.log(`✅ Мигрировано резервов: ${migratedCount}\\n`);\n      \n    } catch (error) {\n      throw new Error(`Ошибка миграции резервов: ${error.message}`);\n    }\n  }\n\n  /**\n   * Инициализация курсов валют\n   */\n  async initializeExchangeRates() {\n    try {\n      console.log('💱 Этап 4: Инициализация курсов валют...');\n      \n      // Получаем актуальные курсы\n      const updateResult = await fiatCurrencyService.updateAllExchangeRates();\n      \n      if (updateResult.success) {\n        this.stats.createdExchangeRates = updateResult.updatedCount;\n        console.log(`✅ Инициализировано курсов валют: ${updateResult.updatedCount}`);\n      } else {\n        console.warn(`⚠️ Частичная инициализация курсов: ${updateResult.error}`);\n        \n        // Создаем fallback курсы\n        await this.createFallbackExchangeRates();\n      }\n      \n      console.log();\n      \n    } catch (error) {\n      throw new Error(`Ошибка инициализации курсов: ${error.message}`);\n    }\n  }\n\n  /**\n   * Создание fallback курсов валют\n   */\n  async createFallbackExchangeRates() {\n    const fallbackRates = {\n      'RUB': 100.0,\n      'EUR': 0.85,\n      'CNY': 7.20,\n      'INR': 83.0,\n      'NGN': 775.0,\n      'VND': 24000.0,\n      'KRW': 1320.0,\n      'JPY': 149.0,\n      'BRL': 5.0\n    };\n\n    let createdCount = 0;\n    \n    for (const [currency, rate] of Object.entries(fallbackRates)) {\n      try {\n        await ExchangeRate.findOneAndUpdate(\n          { baseCurrency: 'USD', targetCurrency: currency },\n          {\n            rate: rate,\n            source: 'fallback',\n            lastUpdated: new Date(),\n            isActive: true,\n            metadata: {\n              apiSource: 'manual',\n              confidence: 0.8\n            }\n          },\n          { upsert: true, new: true }\n        );\n        \n        createdCount++;\n        \n      } catch (error) {\n        this.errors.push(`Ошибка создания fallback курса ${currency}: ${error.message}`);\n      }\n    }\n    \n    console.log(`📦 Создано fallback курсов: ${createdCount}`);\n  }\n\n  /**\n   * Проверка целостности данных после миграции\n   */\n  async validateMigration() {\n    try {\n      console.log('🔍 Этап 5: Проверка целостности данных...');\n      \n      // Проверяем ордера\n      const ordersWithoutCurrency = await P2POrder.countDocuments({\n        currency: { $exists: false }\n      });\n      \n      const ordersWithoutUSD = await P2POrder.countDocuments({\n        pricePerTokenInUSD: { $exists: false }\n      });\n      \n      // Проверяем сделки\n      const tradesWithoutCurrency = await P2PTrade.countDocuments({\n        currency: { $exists: false }\n      });\n      \n      // Проверяем курсы валют\n      const activeExchangeRates = await ExchangeRate.countDocuments({\n        isActive: true\n      });\n      \n      // Проверяем фиатные резервы\n      const totalFiatReserves = await FiatReserve.countDocuments({});\n      const activeFiatReserves = await FiatReserve.countDocuments({\n        status: 'reserved'\n      });\n      \n      console.log('📊 Результаты проверки:');\n      console.log(`  • Ордера без валюты: ${ordersWithoutCurrency}`);\n      console.log(`  • Ордера без USD эквивалента: ${ordersWithoutUSD}`);\n      console.log(`  • Сделки без валюты: ${tradesWithoutCurrency}`);\n      console.log(`  • Активные курсы валют: ${activeExchangeRates}`);\n      console.log(`  • Общее количество фиатных резервов: ${totalFiatReserves}`);\n      console.log(`  • Активные фиатные резервы: ${activeFiatReserves}`);\n      \n      // Проверяем критические ошибки\n      if (ordersWithoutCurrency > 0 || tradesWithoutCurrency > 0) {\n        throw new Error('Обнаружены неполностью мигрированные записи!');\n      }\n      \n      if (activeExchangeRates < 5) {\n        console.warn('⚠️ Предупреждение: Малое количество активных курсов валют');\n      }\n      \n      console.log('✅ Проверка целостности прошла успешно\\n');\n      \n    } catch (error) {\n      throw new Error(`Ошибка проверки целостности: ${error.message}`);\n    }\n  }\n\n  /**\n   * Печать отчета о миграции\n   */\n  printMigrationReport() {\n    console.log('\\n📋 ОТЧЕТ О МИГРАЦИИ');\n    console.log('═'.repeat(50));\n    console.log(`📦 Мигрировано ордеров: ${this.stats.migratedOrders}`);\n    console.log(`🤝 Мигрировано сделок: ${this.stats.migratedTrades}`);\n    console.log(`💰 Мигрировано резервов: ${this.stats.migratedReserves}`);\n    console.log(`💱 Создано курсов валют: ${this.stats.createdExchangeRates}`);\n    \n    if (this.errors.length > 0) {\n      console.log(`\\n❌ Ошибки (${this.errors.length}):`);\n      this.errors.forEach((error, index) => {\n        console.log(`  ${index + 1}. ${error}`);\n      });\n    } else {\n      console.log('\\n✅ Ошибок не обнаружено');\n    }\n    \n    console.log('═'.repeat(50));\n  }\n\n  /**\n   * Откат миграции (аварийная функция)\n   */\n  async rollbackMigration() {\n    try {\n      console.log('🔄 ВНИМАНИЕ: Выполняется откат миграции...');\n      \n      // Удаляем мультивалютные поля из ордеров\n      await P2POrder.updateMany(\n        {},\n        {\n          $unset: {\n            currency: '',\n            pricePerTokenInUSD: '',\n            totalValueInUSD: '',\n            exchangeRateAtCreation: ''\n          }\n        }\n      );\n      \n      // Удаляем мультивалютные поля из сделок\n      await P2PTrade.updateMany(\n        {},\n        {\n          $unset: {\n            currency: '',\n            pricePerTokenInUSD: '',\n            totalValueInUSD: '',\n            exchangeRateAtTrade: ''\n          }\n        }\n      );\n      \n      // Удаляем фиатные резервы\n      await FiatReserve.deleteMany({});\n      \n      // Снимаем флаги миграции с рублевых резервов\n      await RubleReserve.updateMany(\n        { isMigrated: true },\n        {\n          $unset: {\n            isMigrated: '',\n            migratedToFiatReserveId: ''\n          }\n        }\n      );\n      \n      // Удаляем курсы валют\n      await ExchangeRate.deleteMany({});\n      \n      console.log('✅ Откат миграции выполнен');\n      \n    } catch (error) {\n      console.error('❌ Ошибка отката миграции:', error);\n      throw error;\n    }\n  }\n}\n\n// Запуск миграции если скрипт вызван напрямую\nif (require.main === module) {\n  const migration = new P2PMigrationService();\n  \n  // Проверяем аргументы командной строки\n  const args = process.argv.slice(2);\n  \n  if (args.includes('--rollback')) {\n    console.log('⚠️ РЕЖИМ ОТКАТА МИГРАЦИИ');\n    migration.rollbackMigration()\n      .then(() => {\n        console.log('✅ Откат завершен');\n        process.exit(0);\n      })\n      .catch((error) => {\n        console.error('❌ Ошибка отката:', error);\n        process.exit(1);\n      });\n  } else {\n    migration.runMigration();\n  }\n}\n\nmodule.exports = P2PMigrationService;